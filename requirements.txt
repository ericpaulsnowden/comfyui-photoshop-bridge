# torch is deliberately not listed: ComfyUI itself provides it.
watchdog
psd-tools>=1.9.24
pillow>=10.3.0
numpy

# No optional decoders: Load PSD reads .psd/.psb/.tif/.tiff with the above
# alone. Illustrator .ai and camera raw/.dng are opened by Photoshop itself
# (a Tier-2 "Open via Photoshop" node, no third-party lib) -- see
# docs/roadmap/ps-external-decode.md.
